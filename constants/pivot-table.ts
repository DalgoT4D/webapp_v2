// Max row dimensions the UI allows
export const MAX_ROW_DIMENSIONS = 3;

// Max column dimensions the UI allows (same as rows — no artificial cap)
export const MAX_COLUMN_DIMENSIONS = 3;

// Default page size for pivot table pagination (by top-level row groups)
export const PIVOT_DEFAULT_PAGE_SIZE = 50;

// Backend limit for unique column values before rejection
export const MAX_PIVOT_COLUMNS = 100;
