/**
 * KPIConfigDialog tests — stubbed, see KPICard.test.tsx for full rationale.
 * Will be rewritten against the KPI+inline-metric form shape in Batch 10.
 */
describe.skip('KPIConfigDialog (pending Batch 10 rewrite)', () => {
  it('placeholder', () => {});
});
